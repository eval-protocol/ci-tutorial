[pytest]
markers =
    unit: fast, deterministic evals with no external dependencies
    integration: evals that make network calls or depend on external services
    e2e: full end-to-end evals; slow and/or flaky; run on schedule


