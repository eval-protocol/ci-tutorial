[project]
name = "ci-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "eval-protocol>=0.2.17",
    "litellm>=1.75.3",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
]
